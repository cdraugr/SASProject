# SASProject

#### usage:

Прогнать все 300 ссылок (запишет в `data.csv`):

```bash
python main.py
```

---

Поднять веб:

```bash
python -m flask run
```

Поднимет локалхост `http://127.0.0.1:5000/`.

---
